# Python dependencies for Prophet Forecasting API

flask==3.0.0
flask-cors==4.0.0
gunicorn==21.2.0

# Pin numpy to <2.0 for Prophet compatibility
numpy>=1.24.0,<2.0.0
pandas>=2.0.0,<2.2.0

# Prophet with compatible pystan
prophet==1.1.5
