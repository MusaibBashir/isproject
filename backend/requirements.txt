# Python dependencies for Prophet Forecasting API
# Requires Python 3.11 (set in Render dashboard)

# Web framework
flask==3.0.0
flask-cors==4.0.0
gunicorn==21.2.0

# Pin to versions with binary wheels for Python 3.11
numpy==1.26.4
pandas==2.0.3

# Prophet and its Stan backend
cmdstanpy==1.2.0
prophet==1.1.5

# Required for Prophet's cmdstanpy backend
pystan==3.7.0
